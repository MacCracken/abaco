# Abaco Benchmarks

How abaco's hot paths have moved across the toolchain arc. The live last-3-runs
table is [`../bench-latest.md`](../bench-latest.md) (regenerated by
`scripts/bench-history.sh`); the full per-commit trail is
[`../bench-history.csv`](../bench-history.csv).

## 3-point trend (avg ns per call)

Three reference points: **baseline** (2026-04-07, before the 4.8.5 stdlib
math-pack), **optimized** (2026-04-15, after the
[4.8.5 collapse](../../CHANGELOG.md) moved `mod_mul`/`mod_pow` onto hardware
`u64_mulmod`/`u64_powmod`), and **6.0.1** (2026-05-26, the current arc).

| Benchmark | baseline `04-07` | optimized `04-15` | 6.0.1 `05-26` | net |
|-----------|-----------------:|------------------:|--------------:|-----|
| `is_prime_small` | 17000 | 2000 | **1000** | −94% |
| `is_prime_large` | 99000 | 4000 | **4000** | −96% |
| `next_prime` | 24000 | 2000 | **2000** | −92% |
| `factor_small` | 532 | 538 | 551 | ~flat |
| `factor_large` | 3000 | 4000 | 3000 | ~flat |
| `totient` | 456 | 462 | **420** | −8% |
| `fibonacci` | 569 | 595 | **462** | −19% |
| `binomial` | 521 | 521 | **488** | −6% |

**Reading it**: the order-of-magnitude primality wins landed with the 4.8.5
hardware-modmul collapse and **held** across the 5.7.23 → 6.0.1 upgrade;
6.0.1 added a further ~2× on `is_prime_small` and single-digit-percent gains on
the f64 paths. No benchmark regressed across the 2.2.x arc — this is the "prove
no regression held" evidence for the P(-1) hardening pass.

(Times are whole-microsecond-quantized at the small end by the bench harness's
µs rounding; sub-µs f64 ops are reported in ns.)

## Running

```bash
./scripts/bench-history.sh         # appends a CSV row, rewrites bench-latest.md
```

CI runs the `.bcyr` suite non-fatally (numbers vary by runner); the CSV trail is
the regression record. Bench files: `benches/{bench,bench_eval,bench_units}.bcyr`.
